<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Find X</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>keyguard</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>

	<!--
		iOS has no foreground service. An app here is suspended shortly after
		the user leaves it, and the only thing that changes that for this app is
		`bluetooth-central`: with it, CoreBluetooth wakes the app briefly when
		the keyholder connects, disconnects or sends data, which is exactly when
		this app has something to say.

		It is weaker than the Android foreground service and deliberately not
		presented as equal to it: the wake-ups are short, the system decides when
		they happen, and continuous RSSI polling does not survive them. What does
		survive is the disconnect notification — the one that matters most.

		Nothing in Dart needs to know about this. `BackgroundService.isSupported`
		is false here, so the Settings switch is hidden rather than offering a
		control that cannot deliver what it promises.
	-->
	<key>UIBackgroundModes</key>
	<array>
		<string>bluetooth-central</string>
	</array>

	<!--
		Required before CoreBluetooth may be touched at all — an iOS app that
		scans without this string is terminated by the system on first use, so
		these are not optional paperwork. The wording is what the user reads in
		the permission sheet, so it says what the app does with the radio rather
		than naming the API.
	-->
	<key>NSBluetoothAlwaysUsageDescription</key>
	<string>Find X uses Bluetooth to stay connected to your Find Me keyholder and warn you when it moves out of range.</string>
	<key>NSBluetoothPeripheralUsageDescription</key>
	<string>Find X uses Bluetooth to stay connected to your Find Me keyholder and warn you when it moves out of range.</string>

	<!--
		The phone's own GPS, which is what stamps a position on every connect,
		disconnect and ping. "When in use" rather than "always": positions are
		read at the moment the link changes, not tracked continuously.
	-->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Find X records where you were when your keyholder connected or disconnected, so you can find the last place it was with you.</string>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
